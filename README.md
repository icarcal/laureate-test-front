# Laureate front-end test

Install dependencies, then start the webpack process with the following:

```
> git clone https://github.com/icarcal/laureate-test-front.git
> cd laureate-test-front
> npm install
> npm start
```